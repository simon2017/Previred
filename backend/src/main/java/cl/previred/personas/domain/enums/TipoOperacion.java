package cl.previred.personas.domain.enums;

/** Operacion original que quedo pendiente de aplicar en SQL Server, guardada en el outbox. */
public enum TipoOperacion {
    CREATE,
    UPDATE,
    DELETE
}
